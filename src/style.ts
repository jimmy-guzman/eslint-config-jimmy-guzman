import { rules } from './rules/style'

/**
 * https://eslint.org/docs/rules/#layout-formatting
 * https://eslint.org/docs/rules/#suggestions
 * @todo finish documenting rationale
 */
export = {
  extends: ['prettier'],
  rules,
}
